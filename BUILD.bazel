# Hey Emacs, this is -*- coding: utf-8; mode: bazel -*-

load("@rules-compdb//:defs.bzl", "compilation_database")

compilation_database(
  name = "compdb",
  targets = [
    "//packages/binary-gap-cpp14",
    # "//packages/binary-gap-cpp14:binary-gap-cpp14-test",
  ],
  module_exts = ["cpp"],
)
